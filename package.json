{
  "name": "meds-reminder",
  "version": "1.0.0",
  "private": true,
  "description": "A small two-person medication reminder app: a calendar to check off doses, a ping button for reminders, and a confirmation notification when meds are taken.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "generate-vapid-keys": "node server/generate-vapid-keys.js"
  },
  "dependencies": {
    "@libsql/client": "^0.18.0",
    "cookie-session": "^2.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "web-push": "^3.6.7"
  }
}
